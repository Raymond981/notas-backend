version: 2
jobs:
  test:
    docker:
      - image: circleci/node:latest
    steps:
      - checkout
      - run: 
          name: npm install
          command: npm install
      - run:
          name: Test
          command: 'npm test'
      - store_artifacts:
          path: test-results.xml
          prefix: tests
workflows:
  version: 2
  test:
    jobs:
      - test